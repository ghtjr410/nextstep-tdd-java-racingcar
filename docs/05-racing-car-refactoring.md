# 5단계 - 자동차 경주(추가 연습)
***
## 코드 리뷰
> PR 링크:
> 
## 나의 학습 목표
### 1. TDD 사이클을 끝까지 지킨다.
- 구현보다 테스트를 먼저 작성하고
- 작은 실패 -> 작은 성공 -> 리팩토링의 과정을 일관되게 유지한다.
- 테스트 없이 구현하는 습관을 절대 허용하지 않는다.
- 또한 red -> green -> refactor 단위로 커밋한다.
### 2. 객체지향 생활 체조 원칙을 의식적으로 지킨다.
### 3. 속도보다 사고의 깊이를 우선한다.
- 미션을 빠르게 끝내기 위한 타협을 하지않고
- "왜?" 라는 질문을 통해 설계 의도를 명확히 한다.
- 스스로 현재 구현의 의도를 100% 납득하고 설득할 수 있을 때까지 고민해본다.
### 4. 이전 단계의 피드백을 모두 반영한다.
- 1~4단계에서 받은 피드백을 리스트업하여 체크리스트로 관리한다.
- 같은 피드백을 반복하지 않도록, 모든 구현은 사전·사후 체크리스트 점검을 필수로 한다.
### 5. 테스트 범위의 경계를 객체 책임 관점에서 설정한다.
- 무엇을 테스트해야 하고, 무엇은 테스트하지 않아야 하는지를 각 객체의 책임과 협력 구조를 기준으로 판단한다.
- 상태를 검증하는 테스트를 중심으로 구성한다.
### 6. 주생성자와 부생성자를 적극적으로 활용해본다.
- 주생성자는 객체가 반드시 가져야 하는 핵심 상태만 받도록 설계한다.
- 부생성자는 보조적 편의만 제공하도록 제한한다.
## 미션 요구사항
### 1. 지금까지의 자동차 경주 코드 전체 삭제
- 기존 구조나 패턴에 의존하지 않고 완전히 새롭게 출발한다.
### 2. 기능 목록 작성
- 기능을 구현 단위로 나누고
- "누가 어떤 책임을 가져야 하는가"를 중심으로 기능을 해체한다.
### 3. 아무것도 의존하지 않는 가장 작은 단위부터 모델링

## PR 전 자기 점검 체크리스트
- 객체지향 생활체조 원칙을 준수하였는가? 
- TDD 사이클로 구현하였는가?
- TDD 사이클 단위로 커밋하였는가?
- 객체에게 상태를 노출시키지 않고 책임을 맡겼는가?
- 테스트는 객체의 책임과 결과만 검증했는가?
- 불변성을 최대한 유지했는가?
- 도메인 용어로 네이밍했는가?
- 협력 구조가 자연스럽게 읽히는가?

## 3~4 단계 기능 요구사항
- 각 자동차에 이름을 부여할 수 있다. 이름은 5자를 초과할 수 없다.
- 자동차 이름은 쉼표`,`를 기준으로 구분한다.
- 주어진 횟수 동안 n대의 자동차는 전진 또는 멈출 수 있다.
- 전진하는 조건은 0에서 9사이에서 random값을 구한 후 random값이 4이상일 경우이다.
- 전진하는 자동차를 출력할 때 자동차 이름을 같이 출력한다.
- 자동차의 상태를 화면에 출력한다. 어느 시점에 출력할 것인지에 대한 제약은 없다.
- 자동차 경주 게임을 완료한 후 누가 우승했는지를 알려준다. 우승자는 한명 이상일 수 있다.
## 구현 기능 목록

- 자동차
  - [x] 이름으로 자동차 생성
  - [x] 이동 조건에 따라 전진
  - [x] 현재 상태 조회
  - [x] 특정 거리와 같은지 확인
- 자동차 거리
  - [x] 기본값(0)으로 생성
  - [x] 거리 1증가
  - [x] 두 거리 중 최댓값 반환
  - [x] 특정 거리와 같은지 확인
- 자동차 이름
  - [x] 이름 검증
  - [x] 이름 길이 검증
- 자동차 목록
  - [x] 쉼표로 구분된 문자열로 여러 자동차 생성
  - [x] 모든 자동차를 이동 조건에 따라 이동
  - [x] 모든 자동차의 현재 상태 조회
  - [x] 우승자 찾기
- 자동차 상태
  - [x] 이름과 거리로 상태 생성
  - [x] 화면 출력용 문자열 포맷팅
- 경주게임
  - [x] 자동차 목록과 라운드로 게임 생성
  - [x] 이동 조건을 받아 한 라운드 진행
  - [x] 현재 경주 결과 조회
  - [x] 우승자 조회
  - [x] 게임 종료 여부 확인
  - [x] 종료된 게임에서 경주 시도 시 예외 발생
- 경주 라운드
  - [x] 총 라운드 수 검증 (1이상)
  - [x] 다음 라운드로 진행
  - [x] 게임 종료 여부 확인
- 우승자 목록
  - [x] 우승자 목록 검증 (1명 이상)
  - [x] 화면 출력용 문자열 포맷팅 (쉼표로 구분)
- 랜덤 이동 조건
  - [x] 0~9 범위의 랜덤 값 생성
  - [x] 4 이상일 때 이동 가능 판단